/**
 * Сквозная деталь терминала: моно-подпись пути над заголовком секции
 * (~/services, ~/about ...). Эхо строки ~/kirill из карточки-терминала в «Обо
 * мне» — метафора «где ты сейчас в файловой системе сайта». Намеренно
 * приглушённая (muted, нижний регистр) — это указатель пути, а не
 * eyebrow-нумерация; акцент тут не жжём, он остаётся у скролл-спая и каретки.
 */
export function SectionPath({ path }: { path: string }) {
  return <p className="mb-4 font-mono text-xs text-muted">{path}</p>;
}
